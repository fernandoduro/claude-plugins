interface:
  display_name: "PR Merge Review"
  short_description: "Walk a GitHub PR toward a merge decision, one page at a time"
  default_prompt: "Use $walk-through-work-history:pr-merge-review to walk me through this pull request for merge — how it works, the tradeoffs, and the risks to merging — one page at a time."
policy:
  allow_implicit_invocation: true
